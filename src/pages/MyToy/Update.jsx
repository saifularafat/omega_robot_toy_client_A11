import { useParams } from "react-router-dom";

const Update = () => {
    // const
    const { id } = useParams({})
    console.log(id);
    return (
        <div>
            
        </div>
    );
};

export default Update;